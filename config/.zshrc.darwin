export HOMEBREW_CASK_OPTS="--appdir=/Applications"
alias up="brew update && brew upgrade && brew cleanup && brew cask upgrade && gcloud components update --quiet && cargo install-update -a"
export PATH="/usr/local/sbin:$PATH"
