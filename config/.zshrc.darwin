export HOMEBREW_CASK_OPTS="--appdir=/Applications"
alias up="brew update && brew upgrade && brew cleanup && brew cask upgrade && gcloud components update --quiet && anyenv update -v && tldr --update && rustup update nightly && cargo install-update -a"
alias find="mdfind"
export PATH="/usr/local/sbin:$PATH"
