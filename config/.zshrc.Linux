alias up='sudo apt-get update -y && sudo apt-get dist-upgrade -y && sudo apt update -y && sudo apt upgrade -y && sheldon lock --update && gcloud components update --quiet && mise plugins update && tldr --update && rustup update nightly && cargo install-update -a'
alias gist='gist-paste'
# pyenv
export LD_LIBRARY_PATH=~/.anyenv/envs/pyenv/versions/3.9.18/lib
