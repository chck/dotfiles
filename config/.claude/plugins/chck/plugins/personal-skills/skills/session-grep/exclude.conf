# Session file paths matching any of these patterns are dropped before
# classification. One ripgrep regex per line; # starts a comment.
#
# These are logs *about* sessions rather than sessions: whatever string you are
# searching for shows up in them as a side effect of it having been mentioned
# somewhere else, so they crowd out the real answer.
#
# Run with --include-noise to search them anyway.

claude-mem-observer-sessions
